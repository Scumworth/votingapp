{
    "name": "votingapp-node",
    "version": "1.0.0",
    "engines": {
        "node": "7.8.0"
    },
    "scripts": {
        "start": "node server/server.js",
        "heroku-postBuild": "cd react-ui/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build" 
    },
    "cacheDirectories": [
        "node_modules",
        "react-ui/node_modules"
    ],
    "dependencies": {
        "express": "^4.14.1"
    },
    "repository": {
        "type": "git",
        "url": "https://github.com/Scumworth/votingapp"
    },
    "license": "MIT",
    "devDependencies": {
    }
    
}
