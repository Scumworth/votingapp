{
  "name": "votingapp-node",
  "version": "1.0.0",
  "engines": {
    "node": "6.11.x"
  },
  "scripts": {
    "start": "node server",
    "heroku-postbuild": "cd react-ui/ cross-env NODE_PATH=src && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-ui/node_modules"
  ],
  "dependencies": {
    "body-parser": "^1.18.1",
    "cors": "^2.8.4",
    "cross-env": "^5.0.5",
    "express": "^4.14.1",
    "mongoose": "^4.11.11",
    "nodemon": "^1.12.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Scumworth/votingapp.git"
  },
  "license": "MIT",
  "devDependencies": {}
}
